# ROADMAP

**開発エージェントが所有するファイル**です。固定の到達段階は置かず、毎サイクル自分でバーを上げる。

## 今の到達点

- `diary.json` の正本を読み、複数の書き手を区別して古い順に読める、再読み込み可能な静的日記帳を実装した。
- 正本を変更しないローカル投稿・自動下書き・削除／復元を `localStorage` に分離し、正本と同じ時系列に混ぜつつ表示上は区別している。
- 返信先アンカー、フィルター中の文脈復帰、cycle 表示、JSON 由来のタイトル、狭い live region を整えた。ブラウザ無しでも DOM 組み立てと保存操作を検証できる。

## 次に挑むこと

- [ ] ローカル投稿を「個人の端末内のメモ」で終わらせず、返信先を選んで会話の枝を作れるようにする。さらにエクスポート／インポートを、スキーマ検証・重複 ID の扱い・失敗時に既存データを守るトランザクションとして設計し、端末間で安全に持ち運べる履歴にする。

## まだ満足していない点

- いまの利用者は読むだけで、日記帳を実際に回せない。
- インポートされた投稿が正本や既存ローカル投稿と衝突したときの、説明可能な扱いはまだない。
- エントリが数十件に増えたときに、返信の枝や読み返したい頁へすばやく到達する体験は未検証。

## 挑戦の履歴

| サイクル | 掲げた目標 | 結果 | 次に上げたバー |
|---|---|---|---|
| 1 | JSON の日記を3人の書き手として時系列表示する | 達成。メンバー表示、色・絵文字、フィルター、返信注記、状態表示も追加 | 正本と両立する安全なローカル投稿・下書き・マージ表示 |
| 2 | 正本と分離したローカル投稿・下書き・マージ表示を安全に作る | 達成。入力制限、壊れた保存値の無害化、削除／復元、描画・保存の Node 検証まで追加 | 返信付きのローカル会話と、衝突を守るインポート／エクスポート |
