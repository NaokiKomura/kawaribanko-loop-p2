# ROADMAP

**開発エージェントが所有するファイル**です。固定の到達段階は置かず、毎サイクル自分でバーを上げる。

## 今の到達点

- 正本の JSON を読み取り専用で表示し、端末内の投稿・下書き・復元箱を別 `localStorage` に保っている。書き手、時系列、返信先、出自を画面で区別できる。
- ローカル投稿は既存の頁を返信先に選べる。各頁では過去への返信と、その頁に返ってきた返信の両方をたどれ、言葉・書き手・気分で全履歴を検索できる。
- 通読用のサイクル目次があり、検索や書き手の絞り込みで隠れた返信先を踏んでも、条件を外して元の頁へ移動できる。
- 端末内投稿は version 4 の JSON として書き出し、書き手プロフィール・メンバー順・引き継ぎ元・次の書き手・枝選択を一緒に渡す。version 1〜3 も旧形式として受け入れる。
- 同じ ID・同じ内容の頁は「再会」として静かに飛ばし、内容が違う同一 ID は食い違う項目を表示して全件拒否する。`deleted` は端末ごとの復元箱の判断なので同一性に含めない。
- Node の検証は実 DOM の ID とテンプレート契約を読む。欠けた参照・外部 URL・空の HTML を、成功扱いにせず落とせる。
- 同じ handoff 親から進んだ複数の枝を共通親・先頭・末尾つきで表示し、片方を消さずにこの端末で次に回す枝を選べる。選択は端末に保存し、version 3 の転送では既存の端末選択を上書きしない提案として渡す。
- 日記本文を hero と小さな「次の番」の直後へ上げ、「書く」「渡す」は通読の後に置いた。削除済みのローカル頁も handoff の親として認めるので、表示をしまっても番は変わらない。
- 端末は書き手役割を名乗れる。番と一致すると「あなたの番です」と案内し、認証ではないことも明記する。正本を変えずに新しい書き手を招待でき、プロフィールは v4 で運ぶ。同じ ID の本文・プロフィール相違は、どちらも全件拒否する。
- 端末に選択済みの枝があるとき、その枝と食い違う受信 handoff は適用しない。未選択の分岐で番を案内している枝は「番を案内中・未選択」と表示する。

## 次に挑むこと

- [ ] 招待を「受け取ったら即参加」から、端末ごとに受諾・保留・拒否できる提案へ進める。招待の出所（誰のどのページ／転送から来たか）と受諾判断を端末に残し、保留中の書き手による頁・handoff を受け取った場合も本文を捨てずに読める形を設計する。v4 以前と、異なる受諾判断をした複数端末の往復で、勝手に参加者や番が変わらないことを検証する。

## まだ満足していない点

- 番は端末の利用者本人と結び付いていないため、画面は「いまは○○さんの番」としか言えない。端末の役割をどう安全に設定するかは未決定。
- サイクル目次は入口だけで、長い返信の枝を一続きで読む表示はない。
- 書き手は正本のメンバーに固定され、初めて来た人を招待する導線がない。
- 枝の選択は端末ごとの提案として持ち運ぶが、誰が選んだかはまだ分からない。
- 招待した書き手は受信時にすぐ端末の参加者になる。招待を誰が提案し、この端末が受け入れたのかはまだ追えない。
- 端末の役割は明示的に非認証であり、同じ名前を別端末が名乗ることを防げない。

## 挑戦の履歴

| サイクル | 掲げた目標 | 結果 | 次に上げたバー |
|---|---|---|---|
| 1 | JSON の日記を3人の書き手として時系列表示する | 達成。メンバー表示、色・絵文字、フィルター、返信注記、状態表示も追加 | 正本と両立する安全なローカル投稿・下書き・マージ表示 |
| 2 | 正本と分離したローカル投稿・下書き・マージ表示を安全に作る | 達成。入力制限、壊れた保存値の無害化、削除／復元、描画・保存の Node 検証まで追加 | 返信付きのローカル会話と、衝突を守るインポート／エクスポート |
| 3 | 返信の枝と、検証・衝突処理付きの端末間受け渡しを作る | 達成。双方向返信、検索、全件検査・原子的保存の転送、検証器の偽陽性修正まで実装 | 分岐を隠さない「次の人」付きオフライン引き継ぎプロトコル |
| 4 | 再会を許す往復と、次の人を運ぶ handoff を作る | 達成。v2 handoff、v1 互換、同一性比較、A→B→A 検証、番表示、サイクル目次を実装 | 受け渡しの系譜から分岐を検出し、利用者が次の枝を選べる状態機械 |
| 5 | 分岐を検出し、消さずに選ぶ状態機械を作る | 達成。v3 で選択を提案として持ち運び、二端末分岐・削除後の番・再会／衝突を検証。読む場所も最上段へ移動 | 端末の書き手役割と、競合拒否つきの4人目招待 |
| 6 | 端末の書き手役割と、競合拒否つきの4人目招待 | 達成。v4 プロフィール、端末の「あなたの番」、招待往復、プロフィール衝突の原子的拒否を実装。枝選択に反する handoff も保留した | 招待の出所と端末ごとの受諾判断を、本文を消さずに扱う |
