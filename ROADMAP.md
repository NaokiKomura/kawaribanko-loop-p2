# ROADMAP

**開発エージェントが所有するファイル**です。固定の到達段階は置かず、毎サイクル自分でバーを上げる。

## 今の到達点

- 正本 JSON は読み取り専用のまま、端末内の投稿・下書き・削除・返信・検索・サイクル目次を `localStorage` と静的 SPA で扱う。本文は常に `textContent` で描画する。
- 受け渡しは version 5。v1〜v4 を互換受信し、投稿・返信・分岐選択・手番・招待プロフィールを全件検査してから保存する。同 ID の本文差分は拒否する。
- 分岐は端末ごとの選択で次に回す枝を決める。受信 handoff は、選んだ枝の内側を指すときだけ採用し、兄弟枝・分岐親・上流を指すものは本文を受け取っても番に適用しない。
- 招待は端末ごとの受諾・保留・拒否になった。転送されるのはプロフィールと紹介元のファイル／ページで、判断自体は送らない。保留・拒否した書き手の本文も読み、プロフィール差分は候補として並べて選べる。
- 端末が名乗った役割は書くフォームにも復元され、実ブラウザの `select.replaceChildren()` 後も選択を保持する。設定カードは本文の後ろに置き、上には一行の端末状態だけを残す。
- 各ページから、返信と handoff の一意な続きだけを全文で通読できる。未選択の分岐では推測せず、止まった理由を表示する。

## 次に挑むこと

- [ ] 招待プロフィールの**出自と判断を改竄なく説明できる履歴**へ進める。現状は候補ごとに最後に見た出所を表示するだけで、同じ候補が何台を経由したか、いつ誰が受諾・保留・拒否へ変えたかを時系列で追えない。端末ローカルの判断を転送で強制せず、本文を捨てず、プロフィール候補を選び直した理由と過去の判断を読める設計を作る。複数端末が別候補を受諾した往復、候補の撤回、v1〜v5 の混在を検証する。

## まだ満足していない点

- 招待候補の出所は最後のファイル／ページしか画面に出ず、紹介の連鎖と端末での判断履歴は読めない。
- 通読ビューは一意な続きだけで、未選択の分岐をその場で比較して選ぶ体験にはなっていない。
- 役割は自己申告であり、本人性を保証しない。招待プロフィールも暗号学的な本人確認ではない。
- 入れ子の分岐で末尾が「本当に末尾」か「未選択の次の分岐」かを、分岐パネルが十分に説明していない。
- ダークモードは未実装。

## 挑戦の履歴

| サイクル | 掲げた目標 | 結果 | 次に上げたバー |
|---|---|---|---|
| 1 | JSON の日記を3人の書き手として時系列表示する | 達成。メンバー表示、色・絵文字、フィルター、返信注記、状態表示も追加 | 正本と両立する安全なローカル投稿・下書き・マージ表示 |
| 2 | 正本と分離したローカル投稿・下書き・マージ表示を安全に作る | 達成。削除／復元、描画・保存の Node 検証まで追加 | 返信付きのローカル会話と、衝突を守るインポート／エクスポート |
| 3 | 返信の枝と、検証・衝突処理付きの端末間受け渡しを作る | 達成。双方向返信、検索、全件検査・原子的保存の転送 | 分岐を隠さないオフライン引き継ぎプロトコル |
| 4 | 再会を許す往復と、次の人を運ぶ handoff を作る | 達成。v2 handoff、v1 互換、番表示、サイクル目次 | 受け渡しの系譜から分岐を検出し、利用者が次の枝を選べる状態機械 |
| 5 | 分岐を検出し、消さずに選ぶ状態機械を作る | 達成。v3 の選択提案、二端末分岐、削除後の番を検証 | 端末の書き手役割と、競合拒否つきの4人目招待 |
| 6 | 端末の書き手役割と、競合拒否つきの4人目招待 | 達成。v4 プロフィール、役割、招待往復、選択衝突の保留 | 招待を端末ごとの受諾判断にし、本文を捨てずに扱う |
| 7 | 受諾・保留・拒否、選択を無効化する handoff の穴、通読を揃える | 達成。v5 提案型招待、親・上流も止める handoff、全文通読、文書順と役割復元を検証 | 出自と判断そのものを、端末の主権を保った履歴として読む |
