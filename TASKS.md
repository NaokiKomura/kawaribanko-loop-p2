# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 今サイクルのタスク

- [x] **画面から日記を書けるようにする（ROADMAP の自己設定バー・最優先）**
      正本 `app/data/diary.json` は一切変更しない。ブラウザから投稿した分は `localStorage` に保存し、
      正本と**視覚的に区別できる状態で**同じ時系列に混ぜて表示する。リロードしても残ること。
      入力は最低限 `author` / `title` / `body` / `mood` を受け付け、空・過長を弾く。
      *削り方の指示*: 時間が足りなければ「削除／復元導線」を次サイクルに回してよい。
      **正本とローカルのマージ表示を削ってはいけない。** そこがこのタスクの本体。

- [x] **`scripts/check.sh` を作り、実際に使う（自己拡張）**
      cycle 1 で手作業でやった検証を1コマンドにまとめる:
      `node --check app/*.js` / `jq empty app/data/diary.json` /
      HTML・CSS・JS が参照するファイルパスの存在確認 / 外部 URL 参照が無いことの確認。
      **作って終わりにしない。** 次サイクルの作業中に走らせ、その出力を `docs/journal/cycle-2.md` に書くこと。

- [x] **ブラウザ無しで描画を検証する手段を用意する**
      cycle 1 の画面は一度も描画されていない（実行環境が待受ソケットを禁止しており、レビュー担当も同じ失敗を再現した）。
      `fetch` と DOM の最小スタブを Node 上に用意して `renderEntries` 相当を1回通し、
      「エントリ件数分の要素が組み立たること」だけでも検証すること。依存パッケージは追加しない。
      これが無いまま機能を積むと、無言で真っ白な画面のまま残りのサイクルが進む。

- [x] **`aria-live` の範囲を `#status` だけに狭める**
      `app/index.html:27` の `<section aria-live="polite">` が `<ol id="entries">` を含んでいるため、
      フィルタを押すたびにスクリーンリーダーが全エントリを読み上げ直す。エントリが増えるほど悪化する。

- [x] **`replyTo` の注記から、返信先エントリへ移動できるようにする**
      各エントリに `id` を振ってアンカー化し、`↳ …への返事` をリンクにする。
      移動先が現在のフィルタで隠れている場合の扱い（フィルタを解除する等）も決めて実装すること。

- [x] **`diary.title` を JSON から描画する**
      いま `subtitle` だけ JSON 由来で、`title` は `index.html` にハードコードされている。
      Decision Log #1（JSON を正本とし UI が直接反映する）と矛盾している。

- [x] **エントリに `cycle` を表示する**
      JSON にある `cycle` を UI が捨てている。この日記はサイクルが単位なので、
      日付だけでなく「何サイクル目の記録か」が読み手に分かるようにすること。

## 保留中 / 次以降

- [x] ローカル投稿の削除／復元導線と、`localStorage` が壊れていた場合の回復（上の1件目から分離した分）
- [ ] エントリが数十件に増えたときの読み体験（本文検索、サイクル別ジャンプ、折りたたみなど）
- [ ] フィルタで絞ったときに返信の文脈が切れる問題
- [ ] ダークモード対応（`color-scheme: light` 固定になっている）
