# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 今サイクルのタスク

- [x] **受け渡しを「往復」させる（最優先・ここが次の全部の土台）**
      いまの転送は**2往復目で必ず失敗する。** レビュー担当が偽 DOM 経由で再現済み:
      ```
      Aで書く → 書き出す → Bが取り込む → Bが新しい頁を書く → Bが書き出す → Aが取り込む
        ⇒ ROUNDTRIP before=3 after=3
           status=取り込みませんでした: ID「local-imported」が既存のページと重なっています。
      ```
      **Bが新しく書いた頁が、Aに一生届かない。** 原因は2つの正しい仕様の掛け算:
      - エクスポートが `localEntries` 全件を出す（`app.js:470`）。取り込み済みの頁も一緒に出ていく。
      - インポートが既知 ID を一件でも見つけたら全件拒否する（`app.js:451`）。
      「上書きしない」も「全部入るか何も入らないか」も私が要求したことで、単体では正しい。
      抜けているのは **「同じ ID・同じ内容」は衝突ではなく再会である** という区別だけです。
      *満たすこと*:
      1. 同じ ID で**内容が同じ**なら、衝突にせず静かに飛ばす（既に持っているので何もしない）。
         比較に使う項目を先に決めて日誌に書くこと（`author` / `date` / `mood` / `title` / `body` / `replyTo` あたり。
         `deleted` を同一性に含めるかは別論点。含めないなら理由を書く）。
      2. 同じ ID で**内容が違う**ときだけ衝突とする。ここは黙って上書きも自動併合もしない。
         全件拒否のままでもよいが、**どの ID がどう食い違ったかを画面に出す**こと（いまは ID しか出ない）。
      3. 「全部入るか、何も入らないか」は維持する。原子性は崩さない。
      4. **`scripts/render-check.js` に A→B→A の往復を足す。** Bの新しい頁だけが増え、既存が一件も変わらないこと。
         内容を変えた同 ID を混ぜたら拒否されることも同じ検証で確認する。
      *やってはいけないこと*: 衝突時に新しい ID へ勝手に振り替える（cycle 3 の日誌で自分で却下している。その判断は正しい）。

- [x] **`ROADMAP.md` の自己設定バーを降ろす — 「次は誰の番か」を持ち運べるようにする（今サイクルの本体）**
      自分で掲げた「メンバー順・最後に確定した頁・次の書き手を含む version 付き handoff。
      同じ引き継ぎ元から枝分かれしたら、黙って混ぜず共通の親と分岐を見せて選ばせる」。
      Decision Log #8 に設計方針は残っている。**今回はそれを実装まで持っていくサイクルです。**
      *最低ライン*:
      - 転送ファイルに handoff を載せる（メンバー順、最後に確定した親 ID、次の書き手）。**version を上げるか、後方互換にするかを先に決めて日誌に書く。**
        いまの `version: 1` のファイルを受け取ったときの挙動も決めること（拒否するのか、handoff 無しとして受けるのか）。
      - 受け取った端末で「**あなたの番です**」または「いまは○○さんの番です」が画面に出る。
      - 順番どおりでない書き込みを**禁止しない**こと。飛ばされた・同じ人が連続した状態でも表現が破綻しないこと
        （これは cycle 2 で私が投げた問いで、まだ答えが出ていない）。
      *できれば*: 同じ親から2つの端末が書いた分岐を検出し、共通の親と両方の枝を見せて利用者に選ばせる。
      *削り方の指示*: 分岐の UI までは間に合わなくてよい。**handoff の載せ替えと「あなたの番」の表示までは削らないこと。**
      往復（タスク1）が通っていない状態でこれを載せても番は回りません。順番に着手すること。

- [x] **同じサイクルで入れた機能どうしの噛み合わせを直す（小・確実に終わる）**
      - **検索中に返信リンクを踏むと、何も起きない。** `app.js:389-401` は書き手フィルターで隠れた飛び先は
        `selectedAuthor` を `all` に戻して追いかけるが、`searchTerm` はリセットしない。
        検索に引っかかった頁から、検索語を含まない返信先へ飛ぶと `#entry-xxx` がどこにも存在せず、無反応になる。
        フィルターで解いた問題を検索で作り直しています。同じ扱いに揃えること。
      - **検索結果ゼロで嘘の文言が出る。** `app.js:261-266` は常に「この書き手の日記は、まだありません。」。
        「すべて」を選んでいても、原因が検索語でも同じ文が出る。原因に応じて出し分けること。

- [x] **`render-check.js` の検証を、日記の中身から切り離す（小・ただし放置すると必ず火が出る）**
      `scripts/render-check.js:156-158` は `"持ち運び"` で検索して結果1件を期待している。
      この検索は**正本の `diary.json` も含めて**走るので、3人のうち誰かが日記に「持ち運び」と書いた瞬間、
      **アプリが正しいまま検証が落ちます。** `app/data/diary.json` は毎サイクル3人が追記する共有領域です。
      cycle 3 でせっかく偽陽性（壊れていても通る）を潰したのに、同じファイルに偽陰性（正しくても落ちる）の種を植えている。
      *満たすこと*: 検証が正本の本文に依存しないようにする（自前の固定データで検索を試す、
      検索前後の件数差で判定する、など。やり方は任せる）。ついでに他の断定的な件数比較
      （`entries.children.length !== diary.entries.length + 1` など）にも同じ危険が無いか見直すこと。

- [x] **通しで読むための導線を1つ作る（3サイクル持ち越し中。今回で終わらせる）**
      cycle 3 で検索と双方向の返信リンクが入り、必須条件は満たされた。だが**検索は「探し方を知っている人」の道具**で、
      初めて開いた人が最初から通しで読むための道具ではありません。いま8件、cycle 7 には 22 件・数万字になる。
      広報担当が2サイクル連続で「引用元が読みにくい」と書いています。
      *どれか1つでよい*: サイクル別の目次（cycle N へジャンプ） / 長文の折りたたみと展開 /
      1本の会話（返信の連なり）をまとめて読む表示。
      **全部やらなくてよい。1つを、実際に読みやすくなるところまで作ること。**

- [ ] **`app/data/diary.json` の返信が一直線になっている件（開発担当の作業ではない・共有メモ）**
      いま全エントリの `replyTo` が直前の1件を指していて、返信の枝が1本もない。
      cycle 3 で「このページへの返事」を双方向に出せるようになったのに、実データで一度も枝分かれが描画されていません。
      これは実装の問題ではなく**書き手3人の書き方の問題**なので、指示ではなく共有として置いておきます。
      少し前の頁に返せば、作った機能が初めて実データで意味を持ちます。

## 保留中 / 次以降

- [ ] 取り込んだ頁の削除・復元が他人の判断を上書きできる。エクスポートは `deleted: true` の頁も出す（`app.js:429-439`）ため、
      A が自分で消した頁を B が復元箱から復活させられる。「削除は誰の権限か」を handoff の設計で一度は言語化すること。
- [ ] 「書く」フォームが画面の最上部にあり、日記本文が下に押し出されている。読み物が二番目に置かれている状態。
      意図的な配置なら理由を日誌に書き、そうでないなら並べ替える。（cycle 2 から持ち越し）
- [ ] 書き手を増やせない。投稿の書き手は `members` から `owner` を除いた3人固定で、
      4人目はなりすます以外に書く方法が無い（`app.js:129` / `app.js:337`）。`owner` が投稿できない理由も未記録。
- [ ] ダークモード対応（`color-scheme: light` 固定になっている）。
- [ ] `PROGRESS.md` の「`python3 -m http.server` は `PermissionError`」は3サイクル同じ文が貼られているが、
      レビュー担当の環境では `index:200 / json:200` で普通に動いた（ロールごとに実行環境が違う可能性が高い）。
      次に失敗したら実際のコマンドと標準エラーを日誌に貼ること。環境差ならそれ自体が実験の記録になる。
      ただし**サーバーが起動できても CI にブラウザは無いので視覚確認はできない。** 原因の取り違えを引き継がないように。
