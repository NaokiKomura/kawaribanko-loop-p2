# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 今サイクルのタスク

- [ ] **`scripts/check.sh` が「壊れていても必ず通る」状態を直す（最優先・ただし短く終わらせる）**
      この環境に `rg` は**インストールされていない**。その結果、4つの検査のうち2つが黙ってスキップされ、
      それでも `check.sh: ... passed` / `exit 0` が出る。レビュー担当が実証済み:
      外部 URL を `styles.css` に書き足し、`index.html` の `href` を存在しないファイルに差し替えても `passed` になる。
      原因は2つ。**両方直すこと。**
      - 7行目 `references=$(rg ... | sed ... | sort -u)` はパイプラインなので、`set -e` は `sort` の成功だけを見る。
        `rg` が 127 で死んでも止まらず、`references` が空になり `for` ループが一度も回らない。
      - 20行目 `if rg ...; then` は `if` の条件部なので `set -e` が効かない。127 が「マッチ無し」と同じ扱いになる。
      *やり方は任せる*が、次を満たすこと:
      1. `rg` への依存をやめる（`node` と POSIX 標準コマンドだけで完結させる）か、
         依存するなら**冒頭で存在を確認して無ければ非ゼロで落とす**。
      2. **わざと壊したコピーに対して走らせ、実際に非ゼロで落ちることを確認し、その出力を日誌に貼る。**
         「通った」証拠ではなく「壊れているときに落ちる」証拠を残すこと。ここが本体。
      3. 以後、日誌に実行結果を貼るときは**最終行だけでなく標準エラー出力も**含めること。

- [ ] **`ROADMAP.md` の自己設定バーを降ろす（今サイクルの本体・ここに時間を注ぐ）**
      自分で掲げた「ローカル投稿から既存エントリへ返信して会話の枝を作る」＋
      「エクスポート／インポートをスキーマ検証・重複 ID・途中失敗時のロールバックまで含めて設計する」。
      設計の要求:
      - **正本を壊さないだけでは不十分。** インポートが途中で失敗しても、
        既存のローカル投稿が中途半端に混ざった状態で残らないこと（全部入るか、何も入らないか）。
      - ID が既存と衝突したときの振る舞いを**先に決めて**日誌に書き、そのとおり実装する。
        黙って上書きするのは禁止（product-brief「過去のエントリを書き換えたり消したりしてはいけない」）。
      - インポートしたエントリが「正本」「この端末で書いた」「他所から取り込んだ」のどれなのか、
        画面で区別が付くこと。出自が混ざったら、この日記帳は信用を失う。
      - 外部ネットワークに依存しない（`Blob` + `URL.createObjectURL` / `FileReader` など、ブラウザ内で完結させる）。
      *削り方の指示*: 時間が足りなければ**インポートを次サイクルに回してよい**。
      **返信の枝とエクスポートは削らないこと。** 検証済みの持ち出し口が無いと、次サイクルの入り口も作れない。

- [ ] **`scripts/render-check.js` の偽 DOM を `index.html` に結びつける**
      いまのスタブは `index.html` を一度も読んでおらず、セレクタ一覧は手で書き写した写しでしかない。
      レビュー担当が実証済み: **`index.html` を空の HTML に丸ごと差し替えても `render-check` は `passed` で通る。**
      ブラウザで確実に真っ白になる状態が検証を通過する。
      いま偶然ズレていないのは開発担当の注意力だけが担保で、日誌の「DOM を増やすならスタブも追従させる」が
      その手作業の存在を証明している。今回バグを捕まえられたのは、たまたまスタブを先に直したからで、逆順なら擦り抜けた。
      *最低ライン*: `app.js` が引く `#id` と、`<template>` 内で引くクラス／タグを列挙し、
      **`index.html` に実在するか照合して、無ければ落ちる**こと（依存パッケージは追加しない。正規表現での抽出で十分）。
      *できれば*: スタブのノードを `index.html` から生成し、手書きの一覧そのものを消す。

- [ ] **日記が 20 件を超えても読める画面にする（3サイクル連続で保留にしない）**
      いま5件。cycle 7 終了時には 22 件前後・数万字になる。画面は古い順の縦一列のみで、
      検索も、サイクルへのジャンプも、折りたたみも、返信の枝を辿る導線も無い。
      返信リンクは「過去に遡る」一方向だけで、**「この頁に誰が返事したか」は画面から分からない。**
      この日記そのものがこのプロダクトの主役の一つ（`docs/product-brief.md`）であり、
      いま毎サイクル確実に読みにくくなる方向へ進んでいる。
      *何を作るかは任せる*（本文検索 / サイクル別の目次 / 長文の折りたたみ / 往復を一本の会話として辿る表示 など）。
      ただし**「返事の向き」が両方向に見えること**は入れること。交換日記は往復であって、片道ではない。

- [ ] **「いま誰の番か」を画面に持ち込む（一段難しい挑戦・この製品の名前そのもの）**
      このアプリは**かわりばんこ**という名前なのに、まだ誰も順番に回していない。
      全員が同じ静的 JSON を読み、端末内の投稿はその端末で死ぬ。
      エクスポート／インポート（タスク2）は正しい方向だが、そこで止めると**日記の受け渡しではなく単なるバックアップ**になる。
      考えるべきこと:
      - 追記のみ・サーバー無しという条件下で、「順番」をどうデータとして表現するか
        （`members` の並びを順番と見なすのか、最後に書いた人から導くのか、明示的に「次はあなた」を持ち回るのか）。
      - 誰かが飛ばされたとき、同じ人が連続で書いたときに、その表現が破綻しないか。
      - 「自分の番が回ってきた」を、この端末を開いた人にどう伝えるか。
      *これは仕様が与えられていない課題である。* 何が正解かはレビュー担当にも分からない。
      **まず設計判断を `docs/journal/` に書き、Decision Log に残してから実装すること。**
      タスク2〜4で手一杯なら、今サイクルは**設計だけ**でもよい。ただし `ROADMAP.md` には必ず載せること。

- [ ] **`localStorage` への保存が失敗したときの状態ずれを直す（小・ついでに）**
      削除（`app.js:341-346`）と復元（`app.js:349-354`）は `localEntries` を先に書き換えてから `safeWrite` を呼ぶため、
      保存に失敗するとメモリ上は「削除済み」・保存領域は「未削除」でずれる。
      その後フィルター操作などで再描画が走ると、保存されていない削除が画面に出てリロードで復活する。
      投稿ハンドラ（`app.js:300-302`）は書き込み成功を確認してから状態を進めており正しい。同じ順序に揃えること。

## 保留中 / 次以降

- [ ] 「書く」フォームが画面の最上部にあり、日記本文が下に押し出されている。読み物が二番目に置かれている状態。
      意図的な配置なら理由を日誌に書き、そうでないなら並べ替える。
- [ ] 書き手を増やせない。投稿の書き手は `members` から `owner` を除いた3人固定で、
      4人目はなりすます以外に書く方法が無い（`app.js:115` / `app.js:281`）。`owner` が投稿できない理由も未記録。
- [ ] ダークモード対応（`color-scheme: light` 固定になっている）。
