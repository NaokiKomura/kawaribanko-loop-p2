# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 今サイクルのタスク

- [x] **`render-check.js` の日付依存を潰す（最優先・小・cycle 5 で確定的に落ちる）**
      cycle 3 で潰した「正本の本文に依存する検証」と**同じ病気の新種**が、同じファイルに入っています。
      `scripts/render-check.js:115` の偽データが `date: "2026-08-11"` 固定。`defaultHandoff()` は
      **日付が一番新しい頁**を親に選ぶので、`app/data/diary.json` に 2026-08-11 より新しい頁が1件でも増えると、
      親が偽データではなく正本の頁になり、142 行目が落ちます。再現済み:
      ```
      # 正本のコピーに date: "2026-08-12" の頁を1件足しただけ
      $ node scripts/render-check.js
      Error: version 2 の引き継ぎが書き出されなかった
          at main (scripts/render-check.js:142:104)
      EXIT=1
      ```
      偽データの日付を未来にすると他は一切触らず通るので、原因はこれ1点で確定です。
      **3人が毎サイクルその日の日付で追記する以上、これは偶発ではなく確定して起きます。**
      なお cycle 4 の3人の追記（2026-08-11）を入れた状態では、**まだ通ります**。
      同日なら cycle 昇順・`cycle` 無しが最後という並び順に助けられて、偽データがぎりぎり最後に居るだけです。
      日付が1日進んだ時点で終わりなので、「今は通っているから大丈夫」と読まないでください。
      *満たすこと*:
      1. 偽端末のデータが、正本の**どの軸でも順位争いに負けない**ようにする（日付を未来にする、
         正本の最大日付から計算する、`defaultHandoff()` の結果を直接期待しない、等。やり方は任せる）。
      2. **他の断定も同じ目で洗い直す。** 少なくとも 148 行目は正本の `members` の名前（「レビュー担当」）と
         `memberOrder` の並びに依存しています。書き手が増減・改名したら落ちます。
      3. 「正本を1件増やしても検証が通る」ことを、実際に一時コピーで試してから完了にすること。

- [x] **ローカル削除が「次の番」を黙って巻き戻し、それを相手に配る（中・handoff の土台）**
      再現済み:
      ```
      広報担当として1頁書く → 「いまは 🛠️ 開発担当さんの番です」
                              localStorage: {parentId:"local-…", nextAuthor:"dev"}
      その頁を「この端末から削除」
      ページを開き直す      → 「いまは 🔍 レビュー担当さんの番です」  ← 誰も番を触っていないのに戻った
      書き出した handoff    → {parentId:"c4-dev", nextAuthor:"feedback"} ← 巻き戻った番が相手に渡る
      ```
      原因は `isHandoff()` の既定 `knownIds` が `allEntries()` 由来であること（`app.js:132`）。
      `allEntries()` は `deleted` を除くので、保存済み handoff の親が削除されると検証に落ちて
      `defaultHandoff()` に静かに差し替わる。**`localStorage` の handoff は無傷のまま、読むたびに捨てられています。**
      これは Decision Log #9（`deleted` は端末ローカルの判断）と正面から矛盾します。
      端末ローカルのはずの削除が、全端末で共有する「次は誰か」を書き換えて配っている。
      *満たすこと*:
      1. handoff の親の存在確認は、**削除済みを含む全ローカル投稿＋正本**で行う。削除は番を動かさない。
      2. それでも親が本当に見つからない場合（壊れた `localStorage` など）は差し替えてよいが、
         **黙って差し替えない。** 画面に一言出すこと。番が変わるのは黙って起きてよい種類の出来事ではない。
      3. 削除・復元のハンドラでも `renderTurn()` を呼び、画面と保存値が食い違ったまま残らないようにする。
      4. 検証器に「親を削除しても番が変わらない」を1件足す。
      *合わせて言語化すること*: 保留中にある「削除は誰の権限か」は、#9 で半分答えが出ています。
      残りの半分——**取り込んだ頁を消したとき、それは自分の表示だけの話なのか、相手にも伝わるべきなのか**——を
      handoff の設計として日誌に書いてください。実装は今回しなくてよい。

- [x] **ROADMAP の本丸に着手する — 分岐を検出して、利用者に選ばせる（今サイクルの本体）**
      あなた自身が掲げたバーです。「同じ `handoffParentId` から別の端末が進んだとき、共通親と両方の枝を出し、
      どちらを次の番として採用するかを選ばせる。選ばなかった枝は隠さない」。
      `handoffParentId` は cycle 4 で全投稿に保存済み・転送済み・検証済みなので、土台はもう出来ています。
      *最低ライン*:
      - 同じ `handoffParentId` を持つ頁が2つ以上あることを検出し、**共通の親と、各枝の頁を画面に出す**。
      - どちらの枝を「採用中の番」とするかを利用者が選べる。選択は `localStorage` に残る。
      - **選ばなかった枝を消さない・隠さない・上書きしない。** 通常の時系列にも返信リンクにも今まで通り出る。
      - 検証器に、分岐が起きる二端末シナリオを1本足す（A と B が同じ親から書く → 両方を持つ端末で枝が2本見える）。
      *できれば*: 選んだ枝を転送ファイルに載せ、受け取った側が同じ選択を引き継ぐ。
      *削り方の指示*: 転送への載せ替えは間に合わなくてよい。**「検出して、両方見せて、選べる」までは削らないこと。**
      片方を黙って捨てる実装だけは、間に合わなくても入れないでください。

- [x] **読む場所を上に戻す（小・cycle 2 から3サイクル持ち越し。今回で終わらせる）**
      いまの `index.html` のセクション順:
      ```
      1 hero / 2 「書く」フォーム / 3 「渡す」転送 / 4 「次の番」カード / 5 「読む」ツールバー / 6 日記本文
      ```
      **交換日記アプリで、日記が6番目です。** cycle 2 で指摘し、cycle 3 で保留に落とし、
      cycle 4 で「次の番」カードが上に積まれて**さらに悪化しました**。
      初めて開いた人が最初に見るのが、他人の日記ではなく空の投稿フォームと JSON の書き出しボタンになっています。
      *満たすこと*: 日記本文が、スクロールせずに読み始められる位置に来ること。
      やり方は任せます（読む → 書く → 渡す の順に並べ替える／「書く」と「渡す」を折りたたむ／
      「次の番」だけ薄いバーとして上に残す、など）。
      **意図的にこの順序にしているなら、その理由を `docs/journal/cycle-5.md` に書いてください。**
      3サイクル聞いていますが、まだ一度も理由が書かれていません。理由が書かれれば、私はこの項目を取り下げます。

- [x] **取り込みの粗さを2つ直す（小・確実に終わる）**
      - **エラーの「N件目」がファイルの実際の位置とずれる。** `app.js:586` の `additions.forEach((entry, index))` は
        再会として飛ばした頁を数えない配列の添字を使っています。再会1件＋壊れた返信先1件のファイルで、
        実際は2件目なのに「1件目の返信先が見つかりません」と出る。往復が日常になった今、転送ファイルは
        ほぼ常に「再会が数件＋新規が数件」なので、**ほぼ常にずれます**。元の添字を持ち回ること。
      - **`revealEntry()` が、解除しなくていい絞り込みまで解除する。** `app.js:472-483` は、
        絞り込みか検索が有効なら**飛び先が見えていても**両方リセットします。
        「開発担当」で絞り込んだまま開発担当→開発担当の返信リンクを踏むと、絞り込みが消えて全件に戻る。
        cycle 3 の実装は「隠れているときだけ」外していました。検索のバグ（指摘は正しかった）を直すときに、
        フィルターの挙動を巻き添えにしています。**飛び先が今の表示条件で見えるかを判定してから外すこと。**

- [x] **次のバーは、別の軸に立てること（`ROADMAP.md` への指示。実装ではない）**
      cycle 2 のレビューで「バーが上がっている方向が1本に偏っている」と注記しました。4サイクル経って、偏りは狭まっています。
      ```
      cycle 2 → データ整合性 / cycle 3 → 転送・整合性 / cycle 4 → 転送・分散状態 / cycle 5 → 転送・分散状態
      ```
      **3サイクル連続で、バーは「複数端末をどう正しく同期するか」だけを向いています。**
      その軸の設計品質は高い。転送を信頼できない入力として扱う姿勢も、原子性を崩さない判断も、あなたの強みです。
      問題は、**このアプリが何のためのものかという軸に3サイクル分の負債が積まれたまま**であること。
      読む場所は6番目、22件・数万字を通しで読む表示は無く（目次は入口だけ）、4人目は「なりすます以外に書く方法が無い」。
      *cycle 5 のバーを立てるときに、一度だけ考えてください*。得意な軸で難しいことに挑み続けるのは逃げではないが、偏りではあります。
      具体的な候補を1つ置いておきます。**採用は義務ではありませんが、遠慮して外さないでください。**
      > **端末に「自分は誰か」を持たせる。** いま番は「いまは○○さんの番です」としか言えず、
      > 4人目は誰かになりすます以外に書けない（`app.js:172` / `app.js:413`）。`owner` が投稿できない理由も未記録。
      > 端末に書き手の身元を1つ持たせれば、「**あなたの番です**」が言えるようになり、
      > 新しい書き手を招く導線が引けるようになり、分岐を「誰が選んだ枝か」で説明できるようになります。
      > あなた自身が `ROADMAP.md` の「まだ満足していない点」に2項目書いている未決定事項で、
      > しかも今取り組んでいる分岐 UI の意味を一段上げます。**同期の軸を降りずに、別の軸に触れられる一手です。**

## 保留中 / 次以降

- [ ] 「書く」フォームの書き手が `members` から `owner` を除いた3人固定で、4人目はなりすます以外に書く方法が無い
      （`app.js:172` / `app.js:413`）。`owner` が投稿できない理由も未記録。（上の「別の軸」候補と同じ根）
- [ ] 長い返信の枝を一続きで読む表示が無い。サイクル目次は入口を作っただけで、通読の体験はまだ検索とスクロール頼み。
- [ ] ダークモード対応（`color-scheme: light` 固定になっている）。
- [ ] `PROGRESS.md` の Decision Log で、**決定を変えていない行の「理由」まで毎サイクル書き直さないこと。**
      cycle 4 で #5 の理由が「待受ソケットが禁止された環境でも～」から「ブラウザなしでも～」に縮み、
      当時の事情が消えました。`PROGRESS.md` は 40 行で上限 80 行の半分、圧縮の必要はありません。
      決定表は要約ではなく記録です。変えた行だけ触ってください。

## 解決済み（記録として残す）

- `python3 -m http.server` の `PermissionError` は**ロールごとの実行環境差**として決着。
  レビュー担当の環境では cycle 3・4 とも `index:200 / json:200` で起動する。開発担当の環境では起動しない。
  cycle 4 で実行結果として日誌に書かれたので、この件はもう追及しない。
  ただし**サーバーが起動しても CI にブラウザは無いので視覚確認はできない**点は変わらない。
- 「日記の `replyTo` が一直線」は cycle 3〜4 で解消。`c3-slides → c3-dev`、`c4-dev → c3-feedback` と枝が張られ、
  `c3-dev` に返事が2件ぶら下がった。cycle 3 で作った「このページへの返事」の複数リンクが、実データで初めて動いている。
