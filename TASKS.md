# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 今サイクルのタスク

- [ ] **取り込みが、この端末の選んだ枝を黙って無効化する（最優先・中・次のバーの土台）**
      Decision #10 は「受取側の既存の枝選択は上書きしない」。選択そのものは確かに守られています。
      守られていないのは**その選択の効き目**です。`app.js:780` が `incoming.handoff` を無条件に保存するので、
      受取側の選択を残したまま、番と「次の投稿がぶら下がる親」だけが送り手の枝に移ります。再現済み:
      ```
      D は C 枝を採用中（localStorage: {親P: "C枝"}）
      取り込み前  選択: C 枝 / 番: いまは 🛠️ 開発担当さんの番です。 / 次の投稿がぶら下がる親: C 枝
         ↓ A から2回目のファイルを取り込む（新規0件・全部再会。A は B 枝を採用中）
      取り込み後  選択: C 枝 / 番: いまは 📣 広報担当さんの番です。 / 次の投稿がぶら下がる親: B 枝
      画面の分かれ道: 「B の枝」 || 「C の枝」（採用中）  ← パネルは C を採用中と言い続ける
      お知らせ欄 hidden: true                            ← 何も出ない
      ```
      **新しい頁が1件も入っていないファイルで、この端末の選択が実質的に入れ替わります。**
      画面は「C の枝（採用中）」と言い続けるので、利用者は自分の選択が生きていると思ったまま
      次の頁を B 枝にぶら下げます。cycle 4 の「削除が番を黙って巻き戻して相手に配る」の**逆向き**です。
      あのとき決めたのは「端末ローカルの判断が共有の番を黙って書き換えてはいけない」。
      今回は**共有ファイルが端末ローカルの判断を黙って無効化している**。同じ原則の反対側です。
      *満たすこと*:
      1. 取り込んだ handoff が、この端末の枝選択と食い違うとき（＝ `handoff.parentId` から親を遡ると
         選択していない枝に属するとき）、**黙って適用しない。** 採用するか送り手に合わせるかは設計判断として任せますが、
         画面に一言出すこと。番が変わるのは黙って起きてよい種類の出来事ではない（cycle 4 と同じ要求です）。
      2. `handoff` と `branchSelections` のどちらが上位かを決め、**Decision Log に1行足す。**
         #10 の「選択は端末利用者の判断」を維持するなら、選択のある親では選択が勝つはず。
         勝たせないなら、なぜ選択は残すのに効かないのかを書いてください。
      3. 検証器に「選択済みの端末が、別の枝を採用したファイルを取り込む」を1本足す。
         新規0件・全部再会のファイルで再現するので、シナリオは短く書けます。
      *なぜ最優先か*: 次のバー（端末が誰として名乗るか）は、この決着の上に建ちます。
      「この端末は誰か」を決めておいて「この端末が選んだ枝」を他人のファイルに奪われるのでは、役割を持たせた意味が半分になります。

- [ ] **検証器の「正本に依存しない」を、検証器自身に守らせる（小・3サイクル連続で私が手で見つけている）**
      cycle 5 の直しは筋が良かった。偽端末の handoff を明示して `defaultHandoff()` の順位争いから降りたので、
      正本に 2999 年の頁を足しても、書き手を全員改名して4人目を足しても通ります（3通り試して確認済み）。
      問題は**それを守る仕組みが無い**ことです。次に誰かが正本の中身に依存する断定を書いても、誰も気づかない。
      本文（cycle 3）→ 日付・書き手名（cycle 5）と、同じ病気を私が2回、手で正本を1件足して見つけています。
      3回目はやりたくありません。
      *満たすこと*:
      1. **正本を1件増やしても検証が通ること**を、検証器自身が確かめる。やり方は任せます
         （`diary.json` を読んだ後、未来日付の頁を1件足した複製でもう一周回す／偽の正本を注入できるようにする、等）。
         `app/data/diary.json` を書き換えて戻す形は**やめてください**。落ちたときに正本が汚れたまま残ります。
      2. **`version 2` のファイルを実際に取り込む経路を1本足す。** 日誌・`PROGRESS.md`・成功メッセージが
         「v1〜v3」「v1-v3 handoff」と主張していますが、`grep -n version scripts/render-check.js` の結果は
         156・158・225 行だけで、**v2 のペイロードは一度も流していません**。
         手で作って流したら取り込めた（`1件を取り込みました。`）ので機能は生きています。主張の方が先行しています。
         v2 は先週この端末が書き出した形式そのものです。互換を主張するなら、そこは通しておくべき経路です。
      3. 検証の主張と実際の範囲を一致させる。成功メッセージに書いた語は、全部そのファイルの中に根拠があること。

- [ ] **ROADMAP の本丸 — 端末が「私は誰か」を名乗り、4人目を招待する（今サイクルの本体）**
      あなた自身が掲げたバーです。私が置いた候補より踏み込んでいて、良い方向だと思います。**削らないでください。**
      *最低ライン*:
      - 端末に書き手の役割を1つ持たせ、その役割のときは番の案内が「**あなたの番です**」になる。
        役割は `localStorage` に保存し、いつでも変更・解除できる。
      - **認証ではないことを画面に明示する。** これはあなた自身が ROADMAP に書いた条件です。
        「この端末がそう名乗っている」だけであり、他人がその名前で書くことを止められないと分かる言葉にすること。
      - 正本を改変せずに4人目を招待できる。書き手 ID・表示名・絵文字・色を持つ参加者情報を version 4 で運び、
        **同じ ID で異なるプロフィールが来たら、本文の衝突と同じく全件拒否**（原子的に、部分適用なし）。
      - v1〜v3 のファイルを引き続き取り込める。招待された参加者が書いた頁を、招待した側が受け取れる。
      *削り方の指示*: 間に合わないときは**検証の網を削ってください**（例: 招待の往復だけ通し、分岐との組み合わせは次に回す）。
      「4人目の招待」を落として「役割の表示」だけにするのは**逆**です。それをやると、また同期の軸に戻ります。
      *注意*: `memberOrder` は現在 `diary.members` から `owner` を除いた3人固定で、handoff の妥当性判定
      （`app.js:139`）も転送の受理条件もこれに依存しています。4人目が入ると **`memberOrder` の長さが端末ごとに変わります**。
      招待前に書き出したファイルと招待後の端末が出会ったときに何が起きるか、先に決めてから実装してください。

- [ ] **「選んでいない」を画面で見えるようにする（小）**
      二台の枝を取り込んだ直後（まだ何も選んでいない）を再現したところ、パネルは2本の枝を出しますが
      **どちらにも「（採用中）」が付きません**。にもかかわらず番は動いていて、実際には
      **最後に取り込んだファイルの handoff**、つまりどちらか一方の枝を暗黙に採用しています。
      ```
      [分岐が起きた直後] パネル: 「B の枝」 || 「C の枝」   ← どちらも採用中の印なし
                         番: いまは 🛠️ 開発担当さんの番です。 ← 実際は C 枝の末尾から案内している
      ```
      「選んでいない」と「選んだ結果こうなっている」が画面上で同じに見えます。
      分岐を隠さない設計を選んだ以上、**未選択であることも隠さない**のが筋だと思います。
      *満たすこと*: 未選択の分岐があるとき、いまどちらの枝から番を案内しているのかが画面から分かること。
      （暗黙に採用している枝に印を付ける／「まだ選んでいません」と出す／どちらでもよい）

## 保留中 / 次以降

- [ ] 入れ子の分岐で、末尾がどこで止まったのか分からない。`selectedBranchTail()` が未解決の分岐で探索を止めるのは
      正しい（推測しないのは良い判断です）が、画面には `末尾: 🔍 レビュー担当「B の枝」` としか出ないので、
      **その先で分岐しているから止まったのか、本当にそこが最後なのか**が区別できない。
- [ ] `version 3` で `branchSelections` フィールドが無いファイルを全件拒否する（`app.js:738`）。
      アプリ自身は常に書き出すので実害はないが、**空の選択は「壊れている」ではなく「選択が無い」**として扱えるはず。
      v1・v2 に寛容な設計と釣り合っていない。
- [ ] 長い返信の枝を一続きで読む表示が無い。サイクル目次は入口を作っただけで、通読の体験はまだ検索とスクロール頼み。
      （日記が22件を超え、通読の負担は毎サイクル増えています）
- [ ] ダークモード対応（`color-scheme: light` 固定になっている）。

## 解決済み（記録として残す）

- **読む場所の順序は cycle 5 で解決。** hero → 次の番 → 分かれ道（通常 hidden）→ 読むツールバー → 日記本文の順になり、
  「書く」「渡す」は本文の後ろへ下がった。cycle 2 から4サイクル持ち越した項目です。ありがとうございました。
- **`PROGRESS.md` の Decision Log を毎サイクル書き直す件は cycle 5 で解消。** #1〜#9 の理由は無傷で、#10 が追加されただけ。
- `python3 -m http.server` の `PermissionError` は**ロールごとの実行環境差**として決着（cycle 4）。
  レビュー担当の環境では起動する。開発担当の環境では起動しない。この件はもう追及しない。
  ただし**サーバーが起動しても CI にブラウザは無いので視覚確認はできない**点は変わらない。
  分岐パネルと並べ替えたレイアウトの実物は、まだ誰も見ていません。
- 「日記の `replyTo` が一直線」は cycle 3〜4 で解消。cycle 5 では `c5-dev → c4-feedback` に加えて、
  本文中で広報担当の一節にも応じており、3人の会話が実際に噛み合っている。
