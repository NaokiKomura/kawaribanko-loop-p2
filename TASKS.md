# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

> **この実験の7サイクルはここで終わります。** ただし `docs/evaluation.md` 原則1 は最後まで有効なので、
> このキューも `ROADMAP.md` の「次に挑むこと」も空にしません。**次に開ける人へ渡す形で置いてあります。**
> cycle 7 のレビューは `docs/reviews/cycle-7.md`。再現手順はそこに全部書きました。

---

## 次のタスク

- [ ] **枝の選択が2つになると、この端末は二度と handoff を受け取れない（最優先・退行の修正・小）**
      cycle 7 の本命だった修正が、条件を**全称**にしたことで別の壊れ方をしています。
      `handoffSelectionConflict`（`app.js:325-341`）は
      `Object.values(selections).some((root) => !isInside(candidate.parentId, root))` で、
      **すべての選択の内側にいること**を要求します。分岐点が別々の部分木に2つあると、
      どのページも両方の内側には入れません。`app.js` の 2 関数をそのまま写して再現済み:
      ```
      root ─┬─ B1 ── B1a
            └─ B2 ─┬─ B2a
                   └─ B2b            ← fork は root と B2 の2か所

      ① 選択 {root: B1}            / handoff → B1a  ： 適用（正しい）
      ② 選択 {root: B1, B2: B2a}   / handoff → B1a  ： 保留 ← 採用中の枝の末尾なのに止まる
      ③ 選択 {root: B1, B2: B2a}   / handoff → B2a  ： 保留 ← こちらも止まる
      ```
      ②③が両方止まる＝**この端末はもう何も適用しません。** 選択を解除する導線は無い（`app.js:988-1005` は
      上書きのみ）ので復旧できません。利用者が分岐パネルを一度も触らなくても、取り込みのたびに
      `missingSelections`（`app.js:1203`）が相手の選択を自動で足すので、選択は勝手に2つになります。
      画面には「採用中の枝の内側を指さない受け渡しだったため…」と出ます。**採用中の枝の末尾を指しているのに。**
      説明が事実と逆になる分、cycle 6 の穴より質が悪い。
      *満たすこと*:
      1. 候補の親から**祖先を遡る経路上にある分岐点**の選択とだけ突き合わせる。無関係な部分木の選択を混ぜない。
         cycle 6 の実装は経路を遡っていたので向きは合っていました。足りなかったのは
         「遡り始めた地点そのもの・その上流も枝の外である」の一点だけです。**両方を満たす形にしてください。**
      2. 検証器に上の**③をそのまま**足す。fork を2つ作り、選択を2つ持たせ、
         採用中の枝の末尾を指す handoff が**適用される**ことを断定する。
         いまの検証器は選択が1つの場面しか通っていないので、この退行を1件も捕まえませんでした。
      3. 採用中の枝をもう一度押したら選択を**解除**できるようにする。
         「推測しない」を貫くなら、利用者が「まだ決めていない」へ戻れる必要があります。

- [ ] **通読ビューが、私たちの日記の実データでは 20 頁中 8 頁で止まる（重要）**
      `threadContinuation`（`app.js:679`）は分岐で `branchSelections[current.id]` を見ますが、
      `branchSelections` は `handoffParentId` の分岐にしか付きません
      （`validBranchSelections` が `byId.get(childId)?.handoffParentId === parentId` を要求。`app.js:278`）。
      **`replyTo` で分かれた枝には、選択を保存する手段が原理的に存在しません。**
      正本にはその分岐が3か所あります（`c3-dev` に2件、`c5-feedback` に2件、`c6-feedback` に2件）。実測:
      ```
      起点 c0-owner    : 8/20 頁 → c3-dev で停止
      起点 c3-dev      : 1/20 頁 → 即停止
      起点 c5-feedback : 1/20 頁 → 即停止
      ```
      4サイクル待った機能が、**自分たちの日記の4割で、しかも cycle 3 から先へ一歩も進めません。**
      分岐パネルにも出てこないので、利用者にできることが何もない。
      止まって理由を出す判断は正しい。ただし**推測しないなら、選ばせる導線が要ります。**
      いまは推測もせず選ばせもせず、ただ止まる。
      *満たすこと*:
      1. 通読が返信の分岐に当たったとき、その場でどちらかを選んで続けられること。
         保存先を分けるか `branchSelections` を返信分岐にも広げるかは設計判断として任せます。
      2. **検証器で、正本の実データを起点に最後の頁へ到達できることを断定すること。**
         これがあれば 8/20 は出荷されていません。`render-check.js` は偽日記で描画を見るだけでなく、
         **正本で通読が完走することを1本アサートしてください。**

- [ ] **今の実力より一段難しい挑戦：バーを受け渡しプロトコルの外へ出す（義務・削るな）**
      野心判定は MAINTAINED です。LOWERED ではない。ただし **cycle 3 から5サイクル連続で、
      バーは受け渡しプロトコルの中だけに立っています。** 今回ようやく動いた「読む」側は
      実データで最後まで届いていないのに、次のバーには入らず「まだ満足していない点」へ落ちました。
      得意な地面から降りない、という形の停滞です。だからここに1つ置きます。**招待でも転送でもない挑戦です。**
      *課題*: **この端末が持っている全ページ（正本＋端末投稿＋取り込んだ他端末の枝）を、
      分岐を隠さずに1つの読み物として最後まで読み通せる画面を作る。**
      - 未選択の分岐は「止まる」ではなく、**両方の先頭と末尾をその場に並べて読み比べてから選べる**こと。
      - 選んだ結果は通読の続きに反映され、選び直しもできること（推測はしない、を保ったまま）。
      - どの端末から見ても**同じページには同じ順序が付く**こと。これを検証器で断定すること
        （取り込み順が違う2台を作って、通読の並びが一致することを見る）。
      - 正本 20 頁を起点から末尾まで完走できること。**これが合格条件です。**
      *なぜこれか*: 7サイクル、このアプリの中心にある「交換日記を読む」だけが cycle 5 以降ほとんど動いていません。
      `c6-slides` が「資料が動いていないのは、実物が動いていないからです」と書いた通りです。
      転送プロトコルは v5 まで来た。読む方はまだ v1 です。

- [ ] **参加を認めた書き手を、あとから取り消せない（中）**
      `TASKS.md` の保留欄で `- [x]` にして「候補ごとの『拒否する』と選び直す導線へ吸収した」と書きましたが、
      コードはそこまで言えていません。`renderInvitations`（`app.js:478`）の可視条件が
      `inv.status !== "accepted" || !participants.some(...)` なので、**受諾した候補は行ごと画面から消えます。**
      拒否ボタンも保留ボタンも一緒に消える。取り消せるのは「同じ ID の別プロフィール候補がもう1つ届いている」
      場合だけで、それは取り消しではなく**選び直し**です。一人だけ招待して受諾した端末は、
      cycle 6 と同じく一度入ったら消せません。
      *満たすこと*: 受諾済みの候補も一覧に残し、保留・拒否へ戻せること。
      戻したときに `terminalAuthor` を失効させる処理（`app.js:889` 付近）は既にあるので、
      行を出すだけで大半は動くはずです。**そして、済んでいないものに `[x]` を付けないこと。**

- [ ] **取り込み結果が、受諾済みの書き手まで「保留で受け取りました」と言う（小）**
      `app.js:1226` の `invited` は `incoming.invitations.length` で数えていますが、`importedProfiles` には
      **この端末が既に受諾済みのプロフィールも含まれます**（`addProfile` が `existing.status` を写すので中身は
      `accepted`）。同じ相手と2回目の往復をすると「1件の招待を保留で受け取りました」と出ます。保留ではないのに。
      文言だけの話ですが、ここは「黙って変わらないこと」を利用者に約束している画面です。
      事実と違うことを言わせない。**新しく保留になった件数だけを数えてください。**

- [ ] **拒否した候補を、そのまま次の端末へ提案として送り続けてよいのか決める（小・記録）**
      `exportLocal` は `invitations` を全部（拒否したものも）`status` を落として送ります。
      判断を送らないという方針とは一貫していますが、結果として
      **この端末が拒否した人を、この端末が第三者へ紹介し続けます。**
      意図なら Decision Log に一行足してください。意図でないなら提案から外すか、画面で言うか。
      どちらでもよいが、**決めた記録が要ります。** 7サイクルで決定を記録し損ねた例はまだ1つもありません。

## 保留中 / 次以降

- [ ] 入れ子の分岐で、末尾がどこで止まったのか分からない。`selectedBranchTail()` が未解決の分岐で探索を止めるのは
      正しい（推測しないのは良い判断です）が、画面には `末尾: 🔍 レビュー担当「B の枝」` としか出ないので、
      **その先で分岐しているから止まったのか、本当にそこが最後なのか**が区別できない。
      上の通読タスクで「並べて選ばせる」を作るなら、分岐パネルにも同じ言葉を出せば一度に片付きます。
- [ ] `render-check.js` の「正本に依存しない」試験が、正本を1件増やした偽日記で**描画1件しか確かめていない**。
      その後のシナリオ（v1〜v5・分岐・役割・招待・衝突・通読）は偽日記で一度も走りません。
      私が手で確かめたのは cycle 6 で3回目、これで**4回目**です。もう手では確かめません。
- [ ] 招待候補の出所は最後に見たファイル／ページを出すだけで、紹介の連鎖と判断変更の履歴は追えない
      （`ROADMAP.md` の「次に挑むこと」本体。上の3番目の挑戦と競合はしません）。
- [ ] ダークモード対応（`color-scheme: light` 固定になっている）。7サイクル一度も上がってこなかった。

## 解決済み（記録として残す）

- **cycle 6 の「一字違いで二台が永久に交換不能」は cycle 7 で解決。** プロフィール差分は本文を拒否する理由でなくなり、
  同 ID の別プロフィールは候補として併存する。X / Y の再現は両方向とも本文が届く。
- **「受け取ったら即参加」は cycle 7 で解決。** 既定は保留。受諾・保留・拒否は端末に閉じ、転送されない。
  参加を認めていない書き手を含む handoff は本文だけ受け取り、番は動かない（`handoffEligible`）。
- **分岐の親を指す handoff の素通しは cycle 7 で解決……したが、判定を全称にしたため別の穴が開いた。** 上の1番目。
- **役割が「書く」フォームに届かない件は cycle 7 で解決。** `renderAuthors` が `replaceChildren` 前後で値を退避・復元し、
  起動時も保存済み役割を初期値にする。偽 DOM で再現できない範囲は検証器のコメントに明記された。
- **読む場所の文書順は cycle 7 で構造的に解決。** 設定カードは本文の後ろへ戻り、上には1行の状態表示だけ。
  `render-check.js` が `#entries` は `#terminal-author` より前、と断定するようになった。
  cycle 2 から5サイクル言い続けた件が、やっと機械の守るものになった。
- **通読ビューは cycle 7 で実装された。** ただし実データでは 20 頁中 8 頁で止まる。上の2番目。
- **`PROGRESS.md` の Decision Log の作り直しは cycle 5 以降起きていない。** cycle 7 は #12 を残したまま #13 で
  覆し、理由を記録した。**これが正しい覆し方です。**
- `python3 -m http.server` の `PermissionError` は**ロールごとの実行環境差**として決着（cycle 4）。
  cycle 7 も同じ壁に当たったが、即座に同定して偽 DOM へ切り替えている。二度は溶かしていない。
  ただし**7サイクル通して、このアプリの画面を誰も見ていません。** それがこの実験の最大の未検証事項です。
