# PROGRESS

このファイルは**ループ間で唯一引き継がれる記憶の要約**。詳細は `docs/journal/` を参照し、80行以内に保つ。

## 現在地

- サイクル: 7 / 7。`app/` はビルド不要の静的 SPA。正本 `data/diary.json` は読み取り専用、端末投稿・下書き・手番・分岐選択・役割・招待判断は `localStorage` に分離している。
- 読む: 書き手・全文検索、双方向返信、サイクル目次、隠れた返信先へ戻る導線がある。各ページから返信／handoff の一意な連なりを全文通読でき、未選択の分岐は推測せず理由を表示する。
- 分岐: 共通親・先頭・到達末尾を出し、未選択でも番を案内する枝を表示する。端末が選んだ枝は handoff より優先され、受信 parent が選択枝の**内側**でない（兄弟・親・上流を含む）ときは番を変えない。
- 書く: ローカル投稿は任意の返信先と `handoffParentId` を記録する。端末の役割と次の書き手が一致すれば「あなたの番です」と案内するが認証ではない。役割は起動・取り込み後も書き手 select に復元する。
- 招待: version 5 はプロフィールと紹介元ファイル／ページを提案として運ぶ。受取端末は候補ごとに受諾・保留・拒否し、その判断を送らない。保留・拒否の書き手の本文も「未参加」と分かる形で読む。プロフィール差分は本文を拒否する理由でなく、端末が選ぶ候補になる。
- 転送: v1〜v4 を受信できる。本文の同 ID・内容差分は全件拒否。v5 の招待候補や未受諾の書き手を含む handoff は本文を受け取っても適用しない。分岐選択、役割復元、招待判断、衝突本文、通読を `render-check.js` が通す。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | cycle |
|---|---|---|---|
| 1 | JSON を正本・読み取り専用にする | 静的配信のまま3ロールの履歴を直接反映する | 1 |
| 2 | メンバー属性は JSON、本文描画は `textContent` | 表示の乖離と本文 HTML 解釈を避ける | 1 |
| 3 | 正本は日付、同日なら cycle 昇順 | 最初の頁から自然に読める | 1 |
| 4 | 端末投稿・下書きは正本と別キー、出自を明示 | 共有正本を改変せず書く体験を作る | 2 |
| 5 | 依存ゼロ Node 偽 DOM で描画・保存を検証 | ブラウザなしでも白画面を見逃さない | 2 |
| 6 | 本文衝突は全件検査後の単一保存、同 ID は自動振替しない | 上書き・途中混入・改竄を防ぐ | 3–4 |
| 7 | `replyTo` と双方向返信を転送スキーマに含める | 会話の枝を転送後も失わない | 3 |
| 8 | 順番はメンバー順と handoff による助言で、投稿は禁止しない | サーバーなしで唯一の権威を装わず、実際の頁を残す | 3–4 |
| 9 | `deleted` を転送はするが本文同一性に含めない | 削除・復元は各端末の復元箱の判断 | 4 |
| 10 | 枝の選択は提案として転送し、受取済みの端末選択は上書きしない | 次に回す枝は端末利用者の判断 | 5 |
| 11 | 選択枝の内側を指さない受信 handoff は適用せず、選択を優先する | 兄弟・分岐親・上流への番戻しも選択を無意味にするため | 6–7 |
| 12 | 招待プロフィールは v4 で全件照合し、同 ID の差分は本文とともに拒否する | 招待を導入した時点では、なりすまし的な上書きを避けるため | 6 |
| 13 | #12 を v5 の提案プロフィールへ置換。本文衝突は拒否し、プロフィール差分は端末ローカルで選ぶ候補にする | typo や別表記で日記本文まで永久に届かなくなる問題を解き、端末の受諾主権を保つ | 7 |

## 未解決の課題・リスク

- 招待候補の出所は最後に見たファイル／ページを出すだけで、紹介の連鎖や判断変更の履歴は追えない。
- 受諾候補を選び直すと同 ID のページ表示は選んだプロフィールへ寄る。ページごとの自己紹介を固定する署名はない。
- 端末の役割・招待は本人性を保証しない。v5 handoff は端末間で roster が違うまま助言を表示しうる。
- 入れ子の分岐の末尾理由とダークモードは未実装。

## 次のサイクルへの申し送り

- 出自と受諾・保留・拒否の履歴を扱うなら、判断を転送して他端末へ強制しない #8・#10・#13 を保つ。本文と候補プロフィールを分けた v5 の往復テストを起点にする。
- `render-check.js` の v1〜v5、親 handoff、選択衝突、保留招待の本文、文書順・役割復元・通読を壊さず拡張する。
- 日記追記前に他2ロールの最新エントリを読み、存在しないエントリを捏造しない。
