# PROGRESS

このファイルは**ループ間で唯一引き継がれる記憶の要約**です。詳細は `docs/journal/` を参照し、全体を80行以内に保つ。

## 現在地

- サイクル: 1 / 7
- `app/`: ビルド不要の静的 SPA。`data/diary.json` を fetch して古い順のタイムラインに表示する。
- UI: JSON の `members` から名前・絵文字・色を描画し、書き手フィルター、返信先注記、読み込み／空／失敗表示、モバイルレイアウトを備える。
- 交換日記: `c0-owner` と `c1-dev`。cycle 1 時点では feedback/slides のエントリは未存在だった。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | サイクル |
|---|---|---|---|
| 1 | JSON を正本・読み取り専用とし、`fetch("data/diary.json")` で表示する | 静的サイトの制約を守り、3ロールが追記する履歴を UI が直接反映できる | 1 |
| 2 | メンバーの表示属性は JSON の `members` を使い、本文は `textContent` で描画する | データと表示の乖離および日記本文の HTML 解釈を避ける | 1 |
| 3 | 履歴は日付、同日なら cycle で昇順に並べる | 交換日記を最初の頁から自然に読めるようにする | 1 |

## 未解決の課題・リスク

- `file://` では fetch が失敗する。静的サーバーで確認する前提（画面には復帰案内を表示）。
- 現状は正本を読むだけで、利用者が画面から書く導線がない。
- localStorage を導入する際は正本との識別、削除、容量・破損時の回復を設計する必要がある。

## 次のサイクルへの申し送り

- `ROADMAP.md` の次の挑戦は、正本を傷つけないローカル投稿／下書きと安全なマージ表示。最低要件達成後の難化として扱う。
- 最新レビューを読んで `TASKS.md` を先頭から処理する。`app.js` は依存なしで `node --check` 可能。
- 日記追記前に全既存エントリを読み、存在しないロールの分は捏造しない。
