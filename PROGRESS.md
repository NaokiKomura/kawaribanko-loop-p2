# PROGRESS

このファイルは**ループ間で唯一引き継がれる記憶の要約**です。詳細は `docs/journal/` を参照し、全体を80行以内に保つ。

## 現在地

- サイクル: 3 / 7
- `app/`: ビルド不要の静的 SPA。`data/diary.json` を正本として fetch し、正本・端末投稿・他端末からの取り込みを出自付きで時系列表示する。
- 投稿: author/mood/title/body と任意の返信先を検証し、下書きを保存する。ローカル投稿の削除／復元は保存成功後にだけ画面状態を更新する。
- 読む体験: 書き手フィルター、本文・題・書き手・気分の検索、返信先リンクと「この頁への返事」の双方向リンクを備える。リンク先がフィルター外なら全件へ戻ってフォーカスする。
- 転送: ローカル投稿（復元箱を含む）を version 1 JSON に書き出す。取り込みは全件を先に検査し、既存・同梱 ID 衝突または不正な返信先が一つでもあればゼロ件のまま失敗する。成功時のみ一回の `localStorage` 書き込み後に状態を更新する。
- 検証: `scripts/check.sh` は Node だけで構文・JSON・ローカル参照・外部 URL を検査する。`scripts/render-check.js` は実 HTML の ID／テンプレート契約を確認した上で、fetch、返信、下書き、投稿、削除／復元、取り込み、検索を Node 偽 DOM で通す。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | サイクル |
|---|---|---|---|
| 1 | JSON を正本・読み取り専用とし、`fetch("data/diary.json")` で表示する | 静的サイトの制約を守り、3ロールが追記する履歴を UI が直接反映できる | 1 |
| 2 | メンバーの表示属性は JSON の `members` を使い、本文は `textContent` で描画する | データと表示の乖離および日記本文の HTML 解釈を避ける | 1 |
| 3 | 履歴は日付、同日なら cycle で昇順に並べる | 交換日記を最初の頁から自然に読めるようにする | 1 |
| 4 | 端末内の投稿・下書きは正本と別の `localStorage` キーに保存し、投稿は `local: true` として表示する | 静的配信のまま書く体験を作り、共有正本の改変や出自の混同を防ぐ | 2 |
| 5 | ブラウザの代わりに依存ゼロの Node DOM スタブで描画・保存を通す | 待受ソケットが禁止された環境でも、構文検査だけで白画面を見逃さない | 2 |
| 6 | 転送は version 付き JSON、ID 衝突は全件拒否、検査完了後の単一保存とする | 上書きも途中混入も起こさず、正本・端末作成・取り込み済みの出自を説明できるようにする | 3 |
| 7 | `replyTo` を投稿・転送スキーマに含め、各頁に前後両方向の返信リンクを出す | 単なる時系列でなく会話の枝を追え、転送しても文脈を失わない | 3 |
| 8 | 順番は正本メンバー順と最後に確定した handoff を基にした「助言的な次の書き手」として設計する | サーバーなしでは世界で一つの順番を強制できない。分岐は隠さず、共通親から選ばせる方針を次に実装する | 3 |

## 未解決の課題・リスク

- `python3 -m http.server` はこの環境で `PermissionError` となり、実ブラウザの視覚確認は未実施。
- transfer JSON は同じ端末へ再取り込みすると ID 衝突で拒否される。これは追記・非上書きのための意図的な挙動。
- 順番・受け渡しの権威、オフライン分岐の可視化、未知の書き手の招待は未実装。

## 次のサイクルへの申し送り

- handoff では「誰が次か」だけを保存して真実扱いにしない。親 ID、メンバー順、分岐を検査対象にして、衝突を勝手に併合しない UI を設計する。
- `sh scripts/check.sh` と `node scripts/render-check.js` を実装途中・完了時に必ず使う。HTML の ID／テンプレートを変えたら後者が落ちることも確認する。
- 日記追記前に他2ロールの最新エントリを読み、存在しない分は捏造しない。
