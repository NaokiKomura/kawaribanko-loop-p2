# PROGRESS

このファイルは**ループ間で唯一引き継がれる記憶の要約**。詳細は `docs/journal/` を参照し、80行以内に保つ。

## 現在地

- サイクル: 4 / 7
- `app/`: ビルド不要の静的 SPA。正本 `data/diary.json` と、端末内投稿・下書き・handoff を別 `localStorage` に置く。正本は読み取り専用。
- 読む: 書き手・全文検索、双方向返信、サイクル目次、検索や絞り込みで隠れた返信先を表示条件を外して追う導線がある。空結果の理由を区別する。
- 書く: ローカル投稿は任意の返信先と `handoffParentId` を記録し、削除／復元は保存成功後にだけ表示を更新する。順番外の投稿は禁止しない。
- 転送: version 2 は投稿・`memberOrder`・最後の親・次の書き手を運ぶ。version 1 は引き継ぎなしの旧形式として受け入れる。同一 ID でも内容（書き手、日付、気分、題、本文、返信先、引き継ぎ元）が同じなら再会として飛ばし、異なれば差分項目を示して全件拒否する。`deleted` は端末ローカルの復元箱なので同一性に含めない。
- 検証: `scripts/check.sh` は Node だけで構文・JSON・ローカル参照・外部 URL を検査する。`render-check.js` は実 HTML の DOM 契約に加え、正本本文に依存しない偽端末 A→B→A の往復、改変衝突、handoff、検索／返信／目次を通す。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | cycle |
|---|---|---|---|
| 1 | JSON を正本・読み取り専用とし、`fetch("data/diary.json")` で表示する | 静的配信のまま3ロールの履歴を直接反映する | 1 |
| 2 | メンバー属性は JSON、本文描画は `textContent` | 表示の乖離と本文 HTML 解釈を避ける | 1 |
| 3 | 正本は日付、同日なら cycle 昇順 | 最初の頁から自然に読める | 1 |
| 4 | 端末投稿・下書きは正本と別キー、出自を明示 | 共有正本を改変せず書く体験を作る | 2 |
| 5 | 依存ゼロ Node 偽 DOM で描画・保存を検証 | ブラウザなしでも白画面を見逃さない | 2 |
| 6 | 転送は全件検査後の単一投稿保存。内容違いの同 ID は拒否 | 上書き・途中混入・自動 ID 振替を防ぐ | 3–4 |
| 7 | `replyTo` と双方向返信リンクを転送スキーマに含める | 会話の枝を転送後も失わない | 3 |
| 8 | 順番はメンバー順と handoff による助言で、投稿は禁止しない | サーバーなしで唯一の権威を装わず、実際の頁を残す | 3–4 |
| 9 | `deleted` を転送はするが内容同一性に含めない | 削除・復元は各端末の復元箱の判断であり、本文の改竄ではない | 4 |

## 未解決の課題・リスク

- 同じ handoff 親から進んだ分岐をまだ検出・選択できない。`handoffParentId` は保存済みで、次の状態機械の土台。
- handoff は端末利用者と結び付かず、「あなたの番」ではなく「○○さんの番」と案内するだけ。
- 取り込んだ削除済み頁の復元を誰が決めるか、長い返信枝を一続きで読む体験、未知の書き手の招待、ダークモードは未実装。
- `python3 -m http.server 8000 --directory app` は開発環境で `PermissionError: [Errno 1] Operation not permitted`。ブラウザ描画は未確認だが Node 検証は通過。

## 次のサイクルへの申し送り

- handoff の親子関係から分岐を可視化し、利用者が次の枝を選べるようにする。自動併合・上書き・未選択枝の隠蔽はしない。
- 選択状態の持ち運びと、再会／内容相違／削除済み頁を含む複数端末テストを `render-check.js` に足す。
- 日記追記前に他2ロールの最新エントリを読み、存在しないエントリを捏造しない。
