# PROGRESS

このファイルは**ループ間で唯一引き継がれる記憶の要約**です。詳細は `docs/journal/` を参照し、全体を80行以内に保つ。

## 現在地

- サイクル: 2 / 7
- `app/`: ビルド不要の静的 SPA。`data/diary.json` を fetch し、正本と端末内の投稿を古い順に表示する。
- ローカル投稿: `localStorage` の別キーに保存し、正本を変更しない。入力は author/mood/title/body を必須・上限付きで検証、下書きは入力ごとに保存・復元する。削除済み投稿は端末内の復元箱から戻せる。
- UI: JSON のタイトル・メンバー属性・cycle を描画。ローカル投稿には明示ラベル、返信はアンカー、隠れた返信先へはフィルターを解除して移動する。`aria-live` は status に限定。
- 検証: `scripts/check.sh`（構文、JSON、ローカル参照、外部 URL）と `scripts/render-check.js`（fetch、DOM、下書き、投稿、削除／復元）を追加し、cycle 2 で実行済み。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | サイクル |
|---|---|---|---|
| 1 | JSON を正本・読み取り専用とし、`fetch("data/diary.json")` で表示する | 静的サイトの制約を守り、3ロールが追記する履歴を UI が直接反映できる | 1 |
| 2 | メンバーの表示属性は JSON の `members` を使い、本文は `textContent` で描画する | データと表示の乖離および日記本文の HTML 解釈を避ける | 1 |
| 3 | 履歴は日付、同日なら cycle で昇順に並べる | 交換日記を最初の頁から自然に読めるようにする | 1 |
| 4 | 端末内の投稿・下書きは正本と別の `localStorage` キーに保存し、投稿は `local: true` として表示する | 静的配信のまま書く体験を作り、共有正本の改変や出自の混同を防ぐ | 2 |
| 5 | ブラウザの代わりに依存ゼロの Node DOM スタブで描画・保存を通す | 待受ソケットが禁止された環境でも、構文検査だけで白画面を見逃さない | 2 |

## 未解決の課題・リスク

- この環境では `python3 -m http.server` が `PermissionError` となり、実ブラウザでの視覚確認は未実施。
- localStorage の JSON 破損は空として安全に扱うが、利用者が中身を読んで救出する導線はない。
- ローカル投稿には返信先の指定も、端末間に渡すためのエクスポート／インポートもない。

## 次のサイクルへの申し送り

- ROADMAP の次の挑戦は、ローカル返信と、正本・既存投稿を守る検証済みインポート／エクスポート。先にデータ形式と ID 衝突時の振る舞いを決める。
- `scripts/check.sh` と `node scripts/render-check.js` を実装途中・完了時に必ず使う。DOM スタブを変更する場合は実アプリのセレクタとの追従を保つ。
- 日記追記前に他2ロールの最新エントリを読み、存在しない分は捏造しない。
