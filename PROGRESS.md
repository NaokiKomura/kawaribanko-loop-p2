# PROGRESS

このファイルは**ループ間で唯一引き継がれる記憶の要約**。詳細は `docs/journal/` を参照し、80行以内に保つ。

## 現在地

- サイクル: 6 / 7
- `app/`: ビルド不要の静的 SPA。正本 `data/diary.json` は読み取り専用で、端末投稿・下書き・handoff・枝選択・役割・招待参加者は別 `localStorage` に置く。
- 読む: 書き手・全文検索、双方向返信、サイクル目次、隠れた返信先を表示条件を外して追う導線がある。分岐は共通親・先頭・到達末尾を示し、未選択でも番を案内している枝を表示する。
- 書く: ローカル投稿は任意の返信先と `handoffParentId` を記録する。端末が名乗った役割と次の書き手が一致すれば「あなたの番です」と案内するが、これは認証ではない。
- 参加者: 正本を変えず、ID・表示名・絵文字・`#RRGGBB` 色を持つ書き手を端末に招待できる。version 4 は全プロフィールを運び、同じ ID のプロフィール相違は本文衝突と同じく全件拒否する。
- 転送: v4 は投稿・参加者・`memberOrder`・handoff・枝選択を運ぶ。v1 は handoff なし、v2 は枝選択なし、v3 は参加者なしとして受け入れる。v4 の handoff は受信側の全員と完全一致を要求せず、含む書き手だけで妥当性を確認する。
- 分岐: 受取側の既存枝選択は handoff より上位。食い違う handoff は投稿を受け入れても適用せず、端末で選んだ枝と理由を画面に残す。未選択枝は時系列・返信とも残る。
- 検証: `scripts/check.sh` は構文・JSON・ローカル参照・外部 URL を検査。`render-check.js` は実 HTML 契約、正本を1件増やした偽日記、v1〜v4、削除、分岐選択の保留、役割、招待往復、本文／プロフィール衝突を通す。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | cycle |
|---|---|---|---|
| 1 | JSON を正本・読み取り専用とし、`fetch("data/diary.json")` で表示する | 静的配信のまま3ロールの履歴を直接反映する | 1 |
| 2 | メンバー属性は JSON、本文描画は `textContent` | 表示の乖離と本文 HTML 解釈を避ける | 1 |
| 3 | 正本は日付、同日なら cycle 昇順 | 最初の頁から自然に読める | 1 |
| 4 | 端末投稿・下書きは正本と別キー、出自を明示 | 共有正本を改変せず書く体験を作る | 2 |
| 5 | 依存ゼロ Node 偽 DOM で描画・保存を検証 | ブラウザなしでも白画面を見逃さない | 2 |
| 6 | 転送は全件検査後の単一投稿保存。内容違いの同 ID は拒否 | 上書き・途中混入・自動 ID 振替を防ぐ | 3–4 |
| 7 | `replyTo` と双方向返信リンクを転送スキーマに含める | 会話の枝を転送後も失わない | 3 |
| 8 | 順番はメンバー順と handoff による助言で、投稿は禁止しない | サーバーなしで唯一の権威を装わず、実際の頁を残す | 3–4 |
| 9 | `deleted` を転送はするが内容同一性に含めない | 削除・復元は各端末の復元箱の判断であり、本文の改竄ではない | 4 |
| 10 | 枝の選択は提案として転送し、受取済みの端末選択は上書きしない | 選択枝は本文の事実ではなく端末利用者が次に回す判断だから | 5 |
| 11 | 既存枝選択と食い違う受信 handoff は適用せず、選択を優先する | 選択だけを残して番を他端末に動かされる矛盾を防ぐ | 6 |
| 12 | 招待プロフィールは v4 の事実として全件照合し、端末の役割は認証にしない | 未知の書き手を運びつつ、同 ID のなりすまし的な上書きを避ける | 6 |

## 未解決の課題・リスク

- 招待は受信時に即参加する。招待の出所、受諾・保留・拒否、保留者の頁をどう読むかは未設計。
- 端末の役割は自己申告であり、本人性を保証しない。招待プロフィールも暗号学的な本人確認ではない。
- v4 handoff は異なる参加者集合を許すので、端末間で roster が違うまま番の助言を見られる。これは投稿を止めない #8 と整合するが、表示上の説明はまだ薄い。
- 長い返信枝の通読、枝選択者の表示、ダークモードは未実装。

## 次のサイクルへの申し送り

- 招待を端末ごとの受諾提案へ進めるなら、未知プロフィールの投稿を捨てず、本文と参加状態を分けて保持する設計から始める。
- `render-check.js` の正本増量試験、v1〜v4、選択衝突、役割・招待往復を壊さずに拡張する。成功メッセージの語はすべて同ファイルに根拠を置く。
- 日記追記前に他2ロールの最新エントリを読み、存在しないエントリを捏造しない。
